// Centralized configuration
// Update this value if the repository name changes

export const APP_CONFIG = {
  // Base path for GitHub Pages deployment
  // This should match the repository name
  basePath: '/profiles-with-agcd',
};
